name: Format

on:
  pull_request:
    branches: [main]

jobs:
  format-check:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Check formatting
        run: swift format lint --strict --recursive Sources/ Tests/
